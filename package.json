{
  "name": "simple-node-rpi-service",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "bin": "index.js",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "pkg package.json --out-path dist --targets node10-linux-armv7 --no-bytecode",
    "debianize": "mkdir -p deb/usr/bin && cp dist/simple-node-rpi-service deb/usr/bin && dpkg --build deb dist",
    "watch": "nodemon"
  },
  "author": "Kaleb Lape",
  "license": "ISC",
  "dependencies": {
    "micro": "^9.3.3"
  },
  "devDependencies": {
    "nodemon": "^1.18.5",
    "pkg": "^4.3.4"
  }
}
